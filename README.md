# Система автоматической публикации рекламных постов

Система на Python (Telethon) для автоматической публикации рекламных постов в любые группы и каналы через вашу учетную запись Telegram.

## 🚀 Функционал

- ✅ **Публикация в любые каналы и группы** - через вашу учетную запись
- ✅ **Автоматическая публикация** - во все добавленные чаты
- ✅ **Веб-интерфейс** - удобное управление через браузер
- ✅ **Настраиваемый интервал** - через веб-интерфейс
- ✅ **Ручной запуск** - через веб-интерфейс
- ✅ **Управление чатами** - добавление, удаление, просмотр списка
- ✅ **Сохранение данных** - в SQLite базе данных
- ✅ **Планировщик** - с рандомной задержкой между отправками (30-120 сек)
- ✅ **Автовосстановление** - продолжает работу после перезапуска
- ✅ **Автоочистка** - удаляет недоступные чаты из базы
- ✅ **Подробное логирование** - в консоль и файл

## 📁 Структура проекта

```
bot/
├── main.py              # Запуск системы
├── config.py            # Конфигурация (API данные, пути)
├── db.py                # Работа с SQLite
├── scheduler.py         # Планировщик публикации
├── telegram_client.py   # Работа с Telegram Client API
├── web_server.py        # Веб-сервер и API
├── templates/
│   └── index.html       # Веб-интерфейс
├── handlers/
│   └── post.py          # Логика публикации
└── data/
    ├── post.txt         # Текст поста
    ├── image.jpg        # Изображение поста
    ├── database.db      # База данных (создается автоматически)
    └── session.session  # Сессия Telegram (создается автоматически)
```

## 🛠 Установка и настройка

### 1. Установка Docker

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
```

**macOS:**
```bash
# Установите Docker Desktop с официального сайта
# https://www.docker.com/products/docker-desktop
```

**Windows:**
```bash
# Установите Docker Desktop с официального сайта
# https://www.docker.com/products/docker-desktop
```

### 2. Быстрый запуск

```bash
# Клонируем проект
git clone <repository-url>
cd ad_free_tg

# Настраиваем конфигурацию
cp env.example .env
# Отредактируйте .env файл

# Запускаем бота
./docker-run.sh build && ./docker-run.sh start
```

### 3. Получение API данных Telegram

1. Перейдите на [https://my.telegram.org](https://my.telegram.org)
2. Войдите в свой аккаунт Telegram
3. Перейдите в раздел "API development tools"
4. Создайте новое приложение:
   - App title: любое название
   - Short name: любое короткое название
   - Platform: Desktop
5. Сохраните полученные **API ID** и **API Hash**

### 4. Получение ID администратора

1. Напишите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Бот вернет ваш ID - сохраните его

### 5. Настройка конфигурации

```bash
# Копируем пример конфигурации
cp env.example .env

# Редактируем файл .env
nano .env
```

Содержимое файла `.env`:
```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE_NUMBER=+1234567890
ADMIN_ID=123456789
```

### 6. Настройка поста

Отредактируйте файлы с содержимым поста:

**`bot/data/post.txt`** - текст поста (поддерживает HTML-разметку):
```html
🚀 <b>Рекламный пост</b>

Привет! Это автоматический рекламный пост.

📢 <b>Наши преимущества:</b>
• Быстрая доставка
• Качественный сервис

📞 <b>Свяжитесь с нами:</b>
• Telegram: @your_username

#реклама #услуги
```

**`bot/data/image.jpg`** - изображение поста (замените на свое)

## 🚀 Запуск

### Первый запуск (с авторизацией)

```bash
# Интерактивный запуск с вводом кода и пароля
./start-interactive.sh
```

При первом запуске система запросит:
1. 📱 **Код подтверждения** из Telegram
2. 🔑 **Пароль 2FA** (если включена двухфакторная аутентификация)

### Обычный запуск (после авторизации)

```bash
# Сборка и запуск
./docker-run.sh build && ./docker-run.sh start

# Веб-интерфейс будет доступен на http://localhost:5000
```

### Управление через веб-интерфейс

После запуска откройте браузер и перейдите по адресу: **http://localhost:5000**

В веб-интерфейсе доступно:
- 📊 **Мониторинг статуса** - подключение к Telegram, количество групп, интервал
- 🔗 **Управление группами** - добавление, удаление, просмотр списка
- ⚙️ **Настройки** - установка интервала публикации
- 🚀 **Быстрые действия** - публикация сейчас, запуск/остановка планировщика
- 📝 **Управление постом** - перезагрузка содержимого

### Docker команды

```bash
# Просмотр логов
./docker-run.sh logs

# Остановка
./docker-run.sh stop

# Перезапуск
./docker-run.sh restart

# Статус
./docker-run.sh status

# Обновление
./docker-run.sh update
```

## 🔧 Настройка групп

### Добавление групп через веб-интерфейс

Веб-интерфейс поддерживает несколько способов добавления групп:

1. **По username**: `@channel_name`
2. **По ID**: `-1001234567890`
3. **По ссылке**: `https://t.me/channel_name`

### Требования к группам

- Ваша учетная запись должна состоять в группе/канале
- Для каналов ваша учетная запись должна быть администратором
- Для групп достаточно быть участником

## ⚙️ Настройки

### Интервалы публикации

- Минимальный: 1 час
- Максимальный: 8760 часов (365 дней)
- По умолчанию: 24 часа

### Задержки между отправками

- Минимальная: 30 секунд
- Максимальная: 120 секунд
- Случайная задержка для избежания блокировок

## 📊 Мониторинг

### Логи

Логи сохраняются в файл `bot.log` и выводятся в консоль:

```bash
# Просмотр логов в реальном времени
tail -f bot.log

# Просмотр последних 100 строк
tail -n 100 bot.log
```

### Статус бота

Команда `/status` показывает:
- Статус планировщика
- Текущий интервал
- Время следующей публикации
- Количество групп
- Информацию о посте

## 🛠 Устранение неполадок

### Бот не запускается

1. Проверьте токен в `.env`
2. Проверьте ID администратора
3. Убедитесь, что Docker установлен и запущен
4. Проверьте логи: `./docker-run.sh logs`

### Бот не публикует в группы

1. Убедитесь, что ваша учетная запись состоит в группе
2. Проверьте права вашей учетной записи в группе
3. Используйте веб-интерфейс для проверки групп
4. Проверьте логи: `./docker-run.sh logs`

### Группы удаляются из базы

Это нормальное поведение, если:
- Ваша учетная запись была удалена из группы
- Ваша учетная запись заблокирована в группе
- Группа была удалена

## 🔒 Безопасность

- API данные хранятся в переменных окружения
- Веб-интерфейс доступен только локально
- Логи не содержат чувствительной информации
- База данных создается локально

## 📝 Лицензия

Проект создан для образовательных целей. Используйте ответственно и в соответствии с правилами Telegram.

## 🐳 Docker команды

### Основные команды

| Команда | Описание |
|---------|----------|
| `./docker-run.sh build` | Собрать Docker образ |
| `./docker-run.sh start` | Запустить бота |
| `./docker-run.sh stop` | Остановить бота |
| `./docker-run.sh restart` | Перезапустить бота |
| `./docker-run.sh logs` | Просмотр логов |
| `./docker-run.sh status` | Статус контейнеров |
| `./docker-run.sh shell` | Войти в контейнер |
| `./docker-run.sh update` | Обновить и перезапустить |
| `./docker-run.sh clean` | Остановить и удалить все |

### Прямые Docker команды

```bash
# Сборка образа
docker-compose build

# Запуск в фоне
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Статус
docker-compose ps

# Вход в контейнер
docker-compose exec telegram-bot /bin/bash
```

### Мониторинг Docker

```bash
# Использование ресурсов
docker stats telegram-ad-bot

# Логи контейнера
docker logs telegram-ad-bot -f

# Информация о контейнере
docker inspect telegram-ad-bot
```

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи бота: `./docker-run.sh logs`
2. Убедитесь в правильности настроек в `.env`
3. Проверьте права бота в группах
4. Перезапустите бота: `./docker-run.sh restart`
5. Проверьте статус: `./docker-run.sh status`

### Устранение проблем Docker

```bash
# Если контейнер не запускается
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Очистка и пересборка
./docker-run.sh clean
./docker-run.sh build
./docker-run.sh start
```

---

**Важно**: Используйте бота в соответствии с правилами Telegram и не спамьте пользователей!
